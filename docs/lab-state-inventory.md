# 产品与 Lab 状态迁移清单

冻结基线：dev `6dbf338f666f77eefed5a7df76979c8e8b12d6a7`，工作区快照 `900793b`。本表列出字段组及生命周期决策，不声称迁移已完成。产品根指宿主 PLUGIN_DATA（现有 fallback 为 ~/.oxrail），Lab 根为 ~/.oxrail-lab。

| 现有路径 / writer                                                                | 字段及用途                                                                                                                            | 读者                   | 产品必需                                         | 清理时机 / 迁移目标                                                                                                 |
| -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------- | ---------------------- | ------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- |
| setup-verification/{event}.json / state.ts                                       | schemaVersion,event,definitionHash,profileId,sessionDigest,synthetic,observedAt,browserHook,first_browser_hook_seen；近期 Hook 可用性 | doctor                 | 是                                               | 保留产品；按事件覆写，30 秒仅为新鲜度，不是自动删除                                                                 |
| setup-verification/browser-route/\*.json / state.ts                              | schemaVersion,definitionHash,profileId,sessionDigest,synthetic,toolUseDigest,preObservedAt,postObservedAt；首次路由配对               | doctor                 | 首次配对与当前能力标记必需；历史逐调用记录不必需 | 产品保留当前 tuple 最小验证证据；验证完成后停止逐调用积累，历史流水不迁为新证据；Lab 独立采集后续事件               |
| active-profile.json；hosts/\*/profile.json,manifest.json / profile.ts            | profileId,schemaVersion；HostProfile 身份、匹配器、能力及证据绑定；manifest 完整性                                                    | Hook/doctor/Guard      | 是                                               | 保留产品；版本漂移使旧 profile stale，不由 Lab purge 删除                                                           |
| hosts/\* registry bundle / registry-bundle.ts                                    | schema pin, registry manifest,证据hash；生产验证依赖                                                                                  | Guard                  | 是                                               | 保留产品；仅随可信能力更新替换                                                                                      |
| runtime-state/_/_/browser-task-state.json / store.ts                             | session/task摘要、phase、revision、stateVersion、ownership、pending调用、目标epoch、verification marker                               | Core/Hook/Handoff      | 是                                               | 保留产品；任务终结且无租约或待完成调用后由产品生命周期回收                                                          |
| runtime-state tool-call journal / tool-call.ts                                   | call摘要、request/binding摘要、decision、Pre/Post终态、active索引、indexing intent、replay receipt                                    | Hook/Handoff           | 是                                               | 幂等重放及崩溃恢复必需，不能当实验轨迹删除；active项在Post与state提交完成后回收，durable replay保留至作用域安全退休 |
| runtime-state Handoff gate/barrier/lease/lock / handoff-coordinator.ts           | generation、scope摘要、holder、deadline、receipt hash、tombstone、pending mutation                                                    | Handoff/Hook           | 是                                               | 保留产品；仅匹配epoch的验证/取消流程释放；Lab关闭无写权限                                                           |
| credential-fence-state / credential-tool-fence.ts,credential-execution-gate.ts   | generation,current gate,sentinel,lock,active call标记、cleanup回执、quiescence                                                        | 产品凭据边界/Hook      | 是                                               | 保留产品；确认清理后写OPEN终态，不能由Lab清理或超时推定释放                                                         |
| local digest protector key / Core                                                | 本地HMAC密钥代次、绑定摘要；保护调用与动作身份                                                                                        | Core                   | 是                                               | 保留产品；丢失触发现有保守降级，不由实验迁移轮换                                                                    |
| install-state.json / update.ts                                                   | schema、active_root、bundle/Hook/artifact hashes、安装验证状态                                                                        | update/doctor          | 是                                               | 保留产品；更新原子替换，不读写宿主trust store                                                                       |
| experiment-recorder/active.json,sessions/\*.jsonl / experiments/session-recorder | sessionDigest,startedAt；phase,at,browser,elapsedMs,usage计数                                                                         | 实验CLI/Hook adapter   | 否                                               | WP-LAB-001去除产品接入；WP-LAB-002迁入Lab，旧数据仅显式处理，不当新对照                                             |
| ~/.oxrail/credential-demo；native/macos/.build / credential wrapper              | Swift编译缓存、fixture可执行文件                                                                                                      | 内部试验               | 否                                               | WP-LAB-002迁Lab/native及Lab根；旧缓存显式清理，不能删除Keychain或产品状态                                           |
| fixture Keychain item / CredentialDemo                                           | native进程生成的测试值与随机定位符                                                                                                    | native fixture         | 否                                               | 仅fixture自行生命周期清理；Lab迁移不扫描/导出/删除未知Keychain条目                                                  |
| packages/evidence及dist/pilot,verify-evidence,release-gate；evidence runs        | manifest,trace,hash,配对计划、receipt、报告                                                                                           | 开发/审阅              | 否                                               | 迁lab/evidence与lab/dist；历史发布evidence不可变，新run独立创建                                                     |
| benchmarks fixture/reset与native-fidelity输出                                    | 受控初态、reset receipt、后置条件、实验测量                                                                                           | Lab/开发测试           | 否                                               | 保留开发源路径；产物不入产品；新实验运行数据放Lab根                                                                 |
| dist/handoff-control evidence probe                                              | probe构建hash、受控same-tab结果；固定非授权状态                                                                                       | 开发验证               | 否（生产presenter库保留）                        | evidence probe归Lab构建；不把fixture窗口呈现当生产安全交接                                                          |
| host-inventory.json / 操作员                                                     | 精确host/build/browser/tool名称                                                                                                       | setup及Lab各自显式输入 | 输入可必需，开发实例不随产品发货                 | 不自动发现或复制进release；各run绑定独立inventory证据                                                               |

共享纯协议不进行I/O或自动初始化。Lab只可通过可选只读产品适配器补充状态；基本采集链不得依赖该适配器。以上清理策略约束后续实现，现有实现缺少的清理不能标为已完成。
